// MiniCppSyn.cs                                              HDO, 2006-08-28
// -------------
// Table-driven top-down syntax analyzer (G-code interpreter).
// Generated by Coco-2 (PGT).
//=====================================|========================================

// --- call pragma meths. and exec. sem. acts. after syntax errors? ---
#define CONTPRAGMAACTS            // !!!must be defined for SG and PGT!!! 
#undef  CONTSEMACTS               // should not be defined 

using System;
using System.Text;

using Lex = MiniCppLex;
using Sem = MiniCppSem;

public class MiniCppSyn {
                   
  public static String MODULENAME = "MiniCppSyn";

  // --- G-code instructions ---
  public enum Instruction {
    T, TA, TC, TCA, NT, NTA, ANY, ANYA, EPS, EPSA, JMP, RET
  } // Instruction

  private const int ROOTPC     =  918;
  private const int LASTTERM   =   52;
  private const int LASTPRAGMA =   52;


  // --- other constants ---
  private const int MINERRDIST   =   3;
  private const int MAXSTACKLEN  = 100;
  private const int EOFTOK       =   0;
  private const int FIRSTNONTERM = LASTPRAGMA + 1;

  public class NTData {
    public int          startPC;
    public bool         del;
    public Sets.Set256  firstSet;
    public NTData(int startPC, bool del, Sets.Set256 firstSet) {
      this.startPC  = startPC;
      this.del      = del;
      this.firstSet = firstSet;
    } // NTData
  } // NTData
  
  private class GrammarTable {
    public int[]  header = {
      923, 52, 52, 85, 11, 0
    };
    public byte[] code = {
         5,   1,   9,   0,   1,  10,   1,   0,   5,   9,  28,   0,   2,   1,
        20,  18,   0,   2,  51,   3,  16,   4,  32,   5,  10,   1,   0,   1,
        33,  35,   0,  10,   1,   0,   8,   1,  18,  11,   0,  17,   4,   9,
         1,   2,  51,   2,  15,   4,   2,   4,   1,  32,  65,   0,   2,  51,
         5,  18,   4,   2,   7,  10,  51,   0,   0,  33,  11,   0,  34,  13,
         1,  12,  77,   0,  14,  11,   1,  11,  83,   0,  15,  11,   1,  13,
        89,   0,  16,  11,   1,  18,  98,   0,   2,  50,   6,  19,  11,   1,
        19,  93,   0,  17,  10,  93,   0,   5,   2, 111,   0,   1,   1,  32,
       131,   0,   1,  20, 119,   0,   2,  51,   2,  15,   5,   2, 111,   0,
         4,  10, 111,   0,   0,  33,  11,   5,   5, 140,   0,   1,  11,   4,
         6,   2,  11,   0,  33,  11,   4,  10,   1,  11,   0,  23,   5,   8,
       158,   0,   1,   0,  24,  11,   1,   1, 169,   0,   4,  31,   1,  11,
         1,   2, 214,   0,   1,  20, 177,   0,   2,  51,   2,  15,   1,  27,
       187,   0,   0,  28,   1,  32, 211,   0,   4,   9,   4,   1,  20, 198,
         0,   2,  51,   5,  18,   1,  27, 187,   0,   0,  28,  10, 187,   0,
         8,   2,  11,   0,  14,  10, 173,   0,   1,   1, 224,   0,  11,   1,
        14, 229,   0,  11,   0,   2,  11,   0,  25,   5,   1, 242,   0,   1,
        10, 234,   0,   5,   9,   5,   1,   2,   1,  20, 251,   0,   2,  51,
         3,  16,   4,   3,   5,  10, 234,   0,   5,  11,  13,   1,   6,  10,
       234,   0,   0,  26,  11,   5,  12,  23,   1,   1,  25,  11,   5,  13,
        31,   1,   2,  10,  21,   1,   5,  14,  39,   1,   3,  10,  21,   1,
         5,  15,  48,   1,   4,  17,  10,  21,   1,   5,  16,  57,   1,   6,
        19,  10,  21,   1,   5,  17,  65,   1,   8,  10,  21,   1,   5,  18,
        73,   1,   9,  10,  21,   1,   5,  19,  81,   1,  10,  10,  21,   1,
         5,  20,  89,   1,  11,  10,  21,   1,   4,  21,  12,  10,  21,   1,
         0,  33,  11,   4,  10,   1,  11,   4,  22,   1,   0,  33,  11,   0,
         6,  13,   0,  23,   4,  22,   2,   0,  24,   4,  11,   3,   1,   7,
       130,   1,  16,   4,  11,   5,  11,   8,   3,  11,   0,  16,  13,   0,
        23,   4,  22,   2,   0,  24,   4,  11,   3,  11,   0,  15,  13,   0,
        33,  11,   0,   4,  13,   0,  36,  14,   2,  51,   3,  16,   0,  33,
        11,   0,   3,  13,   0,  35,  14,   5,  22, 208,   1,   3,   1,  35,
       205,   1,  19,   5,  22, 190,   1,   8,  10, 177,   1,   3,  52, 199,
         1,   9,  22,  10, 177,   1,   0,   5,  23,  10, 177,   1,   0,  33,
        11,   3,  52, 217,   1,   4,  17,  10, 177,   1,   0,   5,  18,  10,
       177,   1,   0,   9,  13,   0,  27,   0,  28,   2,  51,   2,  15,   0,
        33,  11,   0,  10,  13,   5,  22, 245,   1,   2,   0,  33,  11,   4,
        23,   1,   1,  34,   6,   2,  14,   4,  23,   8,  10, 251,   1,   1,
        37,  14,   2,  15,  10,   0,   2,   1,  38,  22,   2,  16,  10,   0,
         2,   1,  39,  30,   2,  17,  10,   0,   2,   1,  40,  38,   2,  18,
        10,   0,   2,   1,  41,  46,   2,  19,  10,   0,   2,   8,   4,  11,
         4,  24,   1,   1,  44,  63,   2,  14,   4,  24,   3,  10,  52,   2,
         8,   5,  11,   4,  25,   1,   1,  45,  80,   2,  14,   4,  25,   3,
        10,  69,   2,   8,   6,  11,   4,  26,   1,   1,  46,  97,   2,  14,
         4,  26,   8,  10,  86,   2,   1,  47, 105,   2,  15,  10,  91,   2,
         1,  30, 113,   2,  16,  10,  91,   2,   1,  42, 121,   2,  17,  10,
        91,   2,   1,  31, 129,   2,  18,  10,  91,   2,   1,  43, 137,   2,
        19,  10,  91,   2,   8,   7,  11,   1,  18, 170,   2,   4,  27,   2,
        15,   1,  18, 159,   2,  16,   4,  27,   6,  10, 148,   2,   1,  19,
       167,   2,  17,  10, 153,   2,   8,   8,  11,   1,  19, 144,   2,  13,
        10, 144,   2,   4,  28,   1,   1,  20, 192,   2,  14,   4,  28,   5,
        10, 181,   2,   1,  21, 200,   2,  15,  10, 186,   2,   1,  22, 208,
         2,  16,  10, 186,   2,   8,   9,  11,   1,  29, 216,   2,  13,   4,
        29,   2,  11,   1,  12, 226,   2,  13,  11,   1,  11, 232,   2,  14,
        11,   1,  13, 238,   2,  15,  11,   3,  50, 245,   2,   4,  17,  11,
         1,  48,  36,   3,  18,   2,  51,   8,  21,   1,  27,  22,   3,   4,
        22,  10,   0,  28,   1,  48,  13,   3,  24,  11,   1,  49,  19,   3,
        25,  11,   8,  11,  11,   1,  23,   7,   3,   5,  30,  31,   3,  11,
         0,  24,  10,   7,   3,   1,  49,  44,   3,  19,  10, 250,   2,   9,
        10,  51,   3,  10, 250,   2,   1,   8,  67,   3,  26,   4,   9,  15,
         0,  27,   4,  22,  16,   0,  28,  11,   0,  23,   4,  22,  17,   0,
        24,  11,   4,  22,   1,   1,  32,  88,   3,   4,  22,   2,  10,  78,
         3,   8,   2,  11,   9,   2,  96,   3,  11,   1,  20, 100,   3,   2,
        51,   1,  14,   1,  27, 110,   3,   0,  28,   1,  32, 134,   3,   4,
         9,   3,   1,  20, 121,   3,   2,  51,   4,  17,   1,  27, 110,   3,
         0,  28,  10, 110,   3,   8,   2,  11,   5,   3, 143,   3,   1,  11,
         4,   7,   2,   4,   4,   3,  11,   4,   0,   1,   0,   0,  11
    };
    public NTData[] nts = {
      new NTData(   1, true , new Sets.Set256(0x4006, 0x0002, 0x0002)),
      new NTData(  39, false, new Sets.Set256(0x0000, 0x0002)),
      new NTData(  68, false, new Sets.Set256(0x0000, 0x0000, 0x0004)),
      new NTData( 106, false, new Sets.Set256(0x0000, 0x0000, 0x0007)),
      new NTData( 134, false, new Sets.Set256(0x0000, 0x0200, 0x0002)),
      new NTData( 144, false, new Sets.Set256(0x0000, 0x0000, 0x0002)),
      new NTData( 147, false, new Sets.Set256(0x0000, 0x0200)),
      new NTData( 151, false, new Sets.Set256(0x0000, 0x0080)),
      new NTData( 161, false, new Sets.Set256(0x4006)),
      new NTData( 219, false, new Sets.Set256(0x4006)),
      new NTData( 232, false, new Sets.Set256(0x0000, 0x0200)),
      new NTData( 272, false, new Sets.Set256(0xbf58, 0x228d, 0x0002, 0x000f)),
      new NTData( 351, false, new Sets.Set256(0x0000, 0x0000, 0x0002)),
      new NTData( 354, false, new Sets.Set256(0x0000, 0x0200)),
      new NTData( 358, false, new Sets.Set256(0x3900, 0x208c, 0x0000, 0x000f)),
      new NTData( 364, false, new Sets.Set256(0x0040)),
      new NTData( 389, false, new Sets.Set256(0x0000, 0x0001)),
      new NTData( 403, false, new Sets.Set256(0x8000)),
      new NTData( 409, false, new Sets.Set256(0x0010)),
      new NTData( 422, false, new Sets.Set256(0x0008)),
      new NTData( 479, false, new Sets.Set256(0x0200)),
      new NTData( 493, false, new Sets.Set256(0x0400)),
      new NTData( 504, false, new Sets.Set256(0x3900, 0x208c, 0x0000, 0x000f)),
      new NTData( 561, false, new Sets.Set256(0x3900, 0x208c, 0x0000, 0x000f)),
      new NTData( 578, false, new Sets.Set256(0x3900, 0x208c, 0x0000, 0x000f)),
      new NTData( 595, false, new Sets.Set256(0x3900, 0x208c, 0x0000, 0x000f)),
      new NTData( 652, false, new Sets.Set256(0x3900, 0x208c, 0x0000, 0x000f)),
      new NTData( 690, false, new Sets.Set256(0x3900, 0x2080, 0x0000, 0x000f)),
      new NTData( 723, false, new Sets.Set256(0x3900, 0x2080, 0x0000, 0x000f)),
      new NTData( 732, false, new Sets.Set256(0x3900, 0x0080, 0x0000, 0x000f)),
      new NTData( 843, false, new Sets.Set256(0x3900, 0x208c, 0x0000, 0x000f)),
      new NTData( 859, true , new Sets.Set256(0x0000, 0x0010, 0x0000, 0x0008)),
      new NTData( 905, false, new Sets.Set256(0x0000, 0x0080, 0x0007))
    };
    public Sets.Set256[] epsSets = {
      new Sets.Set256(0x0001),
      new Sets.Set256(0x0000, 0x0100),
      new Sets.Set256(0xffde, 0x268f, 0x0002, 0x000f),
      new Sets.Set256(0x0000, 0x1100, 0x000b),
      new Sets.Set256(0x0000, 0x1100, 0x03ef),
      new Sets.Set256(0x0000, 0x1100, 0x13ef),
      new Sets.Set256(0x0000, 0x1100, 0x33ef),
      new Sets.Set256(0x0000, 0xd100, 0xffef),
      new Sets.Set256(0x0000, 0xd10c, 0xffef),
      new Sets.Set256(0x0000, 0x0000, 0x0000, 0x0008),
      new Sets.Set256(0x0000, 0xd17c, 0xffef)
    };
    public Sets.Set256[] anySets = {
      new Sets.Set256(0x0000)
    };
    public String[] names = {
      "end of file", "void", "int", "cout", "cin", "endl", "if", "else",
      "new", "delete", "return", "true", "false", "nullptr", "bool",
      "break", "while", "const", "+", "-", "*", "/", "%", "(", ")", "{",
      "}", "[", "]", "!", "<", ">", ",", ";", "=", "<<", ">>", "+=", "-=",
      "*=", "/=", "%=", "<=", ">=", "||", "&&", "==", "!=", "++", "--",
      "number", "ident", "string", "MiniCpp", "ConstDef", "Init", "VarDef",
      "FuncStuff", "FuncDecl", "FuncDef", "FuncHead", "FormParList",
      "Type", "Block", "Stat", "EmptyStat", "BlockStat", "ExprStat",
      "IfStat", "WhileStat", "BreakStat", "InputStat", "OutputStat",
      "DeleteStat", "ReturnStat", "Expr", "OrExpr", "AndExpr", "RelExpr",
      "SimpleExpr", "Term", "NotFact", "Fact", "ActParList", "VoidRule",
      "LL1Stuff"
    };
  } // GrammarTable;

  private static GrammarTable gt;

  private static int   pc;             // G-code counter
  private static int[] stk;            // stack of G-code return adresses
  private static int   sp;             // stack pointer
  private static bool  mustRead;       // next Lex.token to read
  private static int   altRoot;        // root of alternative chain
  private static bool  leaveOrEnter;   // leaveCnt > 0 OR enterCnt > 0
  private static int   leaveCnt;       // nr. of sem. proc. to leave
  private static int   enterCnt;       // nr. of sem. proc. to enter
  private static int   errDist;        // current error distance
  private static int[] stkAtErr;       // saved stack at error position
  private static int[] newSP;          // new stack length
  private static int[] newPC;          // pc after recovery


  public static void MiniCppSynMethod(Utils.ModuleAction action, out String moduleName) {
  //-----------------------------------|----------------------------------------
    moduleName = MODULENAME;
    switch (action) {
      case Utils.ModuleAction.getModuleName:
        return;
      case Utils.ModuleAction.initModule:
        Errors.InstallStopParsingMethod(StopParsing);
        gt       = new GrammarTable();
        stk      = new int[MAXSTACKLEN];
        stkAtErr = new int[MAXSTACKLEN];
        newSP    = new int[LASTTERM + 1];
        newPC    = new int[LASTTERM + 1];
        break;
      case Utils.ModuleAction.resetModule:
        break;
      case Utils.ModuleAction.cleanupModule:
        gt       = null;
        stk      = null;
        stkAtErr = null;
        newSP    = null;
        newPC    = null;
        break;
    } // switch
  } // MiniCppSynMethod

  private static void Push(int pc) {
    if (sp < MAXSTACKLEN)
      stk[sp++] = pc;
    else
      Errors.Restriction(MODULENAME, "Push", "parser stack overflow");
  } // Push

  private static int Pop() {
    if (sp > 0)
      return stk[--sp];
    else {
      Errors.CompilerError(MODULENAME, "Pop", "parser stack underflow");
      return 0;
    } // else
  } // Pop

  private static void GetNextToken() {
    for (;;) {
      Lex.GetToken(); // updates Lex.token
      if (Lex.token <= LASTTERM)
        return;
  #if !CONTPRAGMAACTS
      if (NumOfSynErrors() == 0)
  #endif
        Sem.pragmaMethods[Lex.token - LASTTERM - 1]();
    } // for
  } // GetNextToken

  private static int AdrAt(int pc) {
    return (gt.code[pc - 1] |
           (gt.code[pc    ] << 8));
  } // AdrAt

  private static void AdjustPC(ref int pc) {
    if (pc == 0)
      return;
    for (;;)
      switch ((Instruction)gt.code[pc - 1]) {
        case Instruction.T:
        case Instruction.TA:
        case Instruction.TC:
        case Instruction.TCA:
        case Instruction.NT:
        case Instruction.NTA:
        case Instruction.ANY:
        case Instruction.ANYA:
        case Instruction.EPS:
        case Instruction.EPSA:
          return;
        case Instruction.JMP:
          pc = AdrAt(pc + 1);
          break;
        case Instruction.RET:
          pc = 0;
          return;
        default:
          pc++; // syn. or sem. action
          break;
      } // switch
  } // AdjustPC

  private static void GetSymInstr(int pc, 
                        out int opcode, out int sy, out int nextPC, out int altPC) {
    opcode = gt.code[pc - 1];
    nextPC = 0; // dummy init
    altPC  = 0; // dummy init 
    switch ((Instruction)opcode) {
      case Instruction.T:
      case Instruction.TA:
      case Instruction.TC:
      case Instruction.TCA:
      case Instruction.ANYA:
      case Instruction.EPS:
      case Instruction.EPSA:
        sy = gt.code[pc];
        break;
      case Instruction.NT:
      case Instruction.NTA:
        sy = gt.code[pc] + FIRSTNONTERM;
        break;
      default: // ANY, JMP, RET, SEM, SYN 
        sy = 0;
        break;
    } // switch
    switch ((Instruction)opcode) {
      case Instruction.T:
      case Instruction.EPS:
        nextPC = pc + 2;
        altPC = 0;
        break;
      case Instruction.TC:
      case Instruction.NT:
        nextPC = pc + 3;
        altPC = 0;
        break;
      case Instruction.TA:
      case Instruction.ANYA:
      case Instruction.EPSA:
        nextPC = pc + 4;
        altPC = AdrAt(pc + 2);
        break;
      case Instruction.TCA:
      case Instruction.NTA:
        nextPC = pc + 5;
        altPC = AdrAt(pc + 2);
        break;
      case Instruction.ANY:
        nextPC = pc + 1;
        altPC = 0;
        break;
    } // switch
    AdjustPC(ref nextPC);
    AdjustPC(ref altPC);
  } // GetSymInstr

  private static void Fill(int pc, int sp) {
    int opcode, sy, nextPC, altPC, i;
    AdjustPC(ref pc);
    while (pc > 0) {
      GetSymInstr(pc, out opcode, out sy, out nextPC, out altPC);
      switch ((Instruction)opcode) {
        case Instruction.T:
        case Instruction.TA:
        case Instruction.TC:
        case Instruction.TCA:
          newPC[sy] = pc;
          newSP[sy] = sp;
          break;
        case Instruction.NT:
        case Instruction.NTA:
          for (i = 0; i <= LASTTERM; i++)
            if (Sets.member((ushort)i, gt.nts[sy - FIRSTNONTERM].firstSet)) {
              newPC[i] = pc;
              newSP[i] = sp;
            }
          if (gt.nts[sy - FIRSTNONTERM].del)
            Fill(nextPC, sp);
          break;
        case Instruction.EPS:
        case Instruction.EPSA:
          Fill(nextPC, sp);
          break;
      } // switch
      pc = altPC;
    } // while
  } // Fill

  private static void FillSucc(int pc, int sp) {
    int opcode, sy, nextPC, altPC;
    AdjustPC(ref pc);
    while (pc > 0) {
      GetSymInstr(pc, out opcode, out sy, out nextPC, out altPC);
      if (nextPC > 0)
        Fill(nextPC, sp);
      pc = altPC;
    } // while
  } // FillSucc

  private static void Triple(int altRoot) {
    for (int i = 0; i <= LASTTERM; i++) { // clear triple list
      newPC[i] = 0;
      newSP[i] = 0;
    } // for
    for (int i = 1; i <= sp; i++) {       // successors of stacked nt's
      FillSucc(stk[i - 1], i - 1);
      Fill(stk[i - 1], i - 1);
    } // for
    FillSucc(altRoot, sp);                // successors of alt. chain
    Fill(altRoot, sp);                    // current alt. chain
  } // Triple

  private static void HandleSynErr(ref int pc, ref int altRoot) {
    int     opcode, sy, nextPC, altPC, adjPC, nwSP, eq;
    bool    nameInMsg;
    String  name;
    StringBuilder msg = new StringBuilder();
    leaveOrEnter = false;
    if (errDist >= MINERRDIST) { // --- primary error
      nameInMsg = false;
      adjPC = altRoot;
      AdjustPC(ref adjPC);
      while (adjPC > 0) {
        GetSymInstr(adjPC, out opcode, out sy, out nextPC, out altPC);
        if ( (Instruction)opcode >= Instruction.T  && 
             (Instruction)opcode <= Instruction.NTA  ) {
          if ( gt.names[sy].IndexOf(' ') >= 0 || 
               !Char.IsLetter(gt.names[sy][0]) )
            name = "\"" + gt.names[sy] + "\"";
          else
            name = gt.names[sy];
          if (name.IndexOf('{') > -1) // prevent formatting problems in Errors.SynError
            name = name.Replace("{", "{{");
          if (name.IndexOf('}') > -1) // prevent formatting problems in Errors.SynError
            name = name.Replace("}", "}}");
          if (msg.Length + name.Length + 2 > 80) {
            msg.Append(" ...");
            break;
          } // if
          if (!nameInMsg) {
            nameInMsg = true;
            msg.Append("expected: " + name);
          } else {
            msg.Append(", ");
            msg.Append(name);
          } // else
        } // if
        adjPC = altPC;
      } // while
      if (!nameInMsg)
        msg.Append("syntax error");
      Errors.SynError(Lex.tokenLine, Lex.tokenCol, msg.ToString(), "");
      Triple(altRoot);
      while (newPC[Lex.token] == 0) {
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter)
          return;
      } // while
      nwSP = newSP[Lex.token];
      leaveCnt = sp - nwSP;
      enterCnt = 0;
      Array.Copy(stk, stkAtErr, stk.Length); // save stack
    } else {                 // --- spurios error
      if (errDist == 0) {    // no token used since last error
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter)
          return;
      } // if
      while (newPC[Lex.token] == 0) {
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter) 
          return;
      } // while
      nwSP = newSP[Lex.token];
      eq = 2;                // compare stkAtErr and stk
      while (eq <= sp && eq <= nwSP && stkAtErr[eq - 1] == stk[eq - 1])
        eq++;
      eq--;                  // stacks are equal up to index eq
      leaveCnt = sp - eq;
      if (nwSP < eq) {
        leaveCnt = sp - nwSP;
        enterCnt = 0;
      } else if (nwSP > eq)
        enterCnt = nwSP - eq;
      else // nwSP == eq
        enterCnt = 0;
      Array.Copy(stkAtErr, stk, stk.Length); // restore stack
    } // else
    leaveOrEnter = (leaveCnt + enterCnt) > 0;
    sp = nwSP;
    pc = newPC[Lex.token];
    altRoot = pc;
    errDist = 0;
  } // HandleSynErr

  public static int Interpret() {
  //-----------------------------------|----------------------------------------
    int actionNr, opcode, currPC;
    for (;;) {
      opcode = gt.code[pc - 1];
      if ( !leaveOrEnter && mustRead && 
           (Instruction)opcode <= Instruction.EPSA ) {
        GetNextToken();
        mustRead = false;
        errDist++;
        altRoot = pc;
      } // if
      if (leaveOrEnter) {
        if (leaveCnt > 0) {
          leaveCnt--;
          actionNr = 0;
        } else { // enterCnt > 0
          enterCnt--;
          actionNr = gt.code[stkAtErr[sp - enterCnt - 1] - 2];
        } // else
        leaveOrEnter = (leaveCnt + enterCnt) > 0;
        return actionNr;
      } // if
      pc++;
      switch ((Instruction)opcode) {
        case Instruction.T:
          if (Lex.token == gt.code[pc - 1])
            if (Lex.token != EOFTOK) {
              pc++;
              mustRead = true;
            } else {
              return 0;
            } // else
          else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.TA:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 3;
            mustRead = true;
          } else
            pc = AdrAt(pc + 1); // try alternative
          break;
        case Instruction.TC:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 2;
            mustRead = true;
            actionNr = gt.code[pc - 2];
            return actionNr;
          } else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.TCA:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 4;
            mustRead = true;
            actionNr = gt.code[pc - 2];
            return actionNr;
          } else
            pc = AdrAt(pc + 1); // try alternative
          break;
        case Instruction.NT:
          currPC = gt.code[pc - 1];
          if ( gt.nts[currPC].del || 
               Sets.member((ushort)Lex.token, gt.nts[currPC].firstSet) ) {
            pc++;
            actionNr = gt.code[pc - 1];
            Push(pc + 1);
            pc = gt.nts[currPC].startPC;
            altRoot = pc;
            return actionNr;
          } else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.NTA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.nts[currPC].firstSet)) {
            pc += 3;
            actionNr = gt.code[pc - 1];
            Push(pc + 1);
            pc = gt.nts[currPC].startPC;
            altRoot = pc;
            return actionNr;
          } else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.ANY:
          mustRead = true;
          break;
        case Instruction.ANYA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.anySets[currPC - 1])) {
            pc += 3;
            mustRead = true;
          } else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.EPS:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.epsSets[currPC - 1]))
            pc++;
          else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.EPSA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.epsSets[currPC - 1]))
            pc += 3;
          else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.JMP:
          pc = AdrAt(pc);
          break;
        case Instruction.RET:
          pc = Pop();
          altRoot = pc;
          return 0;
        default:
          if (opcode <= 128) {
  #if !CONTSEMACTS
          if (Errors.NumOfSynErrors() == 0)
  #endif
            return opcode - 12;
          } else // opcode > 128
            return opcode - 128;
          break;
      } // switch
    } // for
  } // Interpret


  public static bool Parse() {
  //-----------------------------------|----------------------------------------
    Lex.InitLex();
    sp           = 0;
    pc           = ROOTPC;
    altRoot      = pc;
    mustRead     = true;
    errDist      = MINERRDIST + 1;
    leaveOrEnter = false;
    leaveCnt     = 0;
    enterCnt     = 0;
    Sem.StartSem();
    return Errors.NumOfSynErrors() == 0;
  } // Parse


  public static void StopParsing() {
  //-----------------------------------|----------------------------------------
    leaveCnt     = sp + 1;
    enterCnt     = 0;
    leaveOrEnter = true;
  } // StopParsing

} // MiniCppSyn
 
// End of MiniCppSyn.cs
//=====================================|========================================