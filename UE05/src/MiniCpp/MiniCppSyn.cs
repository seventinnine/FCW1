// MiniCppSyn.cs                                              HDO, 2006-08-28
// -------------
// Table-driven top-down syntax analyzer (G-code interpreter).
// Generated by Coco-2 (PGT).
//=====================================|========================================

// --- call pragma meths. and exec. sem. acts. after syntax errors? ---
#define CONTPRAGMAACTS            // !!!must be defined for SG and PGT!!! 
#undef  CONTSEMACTS               // should not be defined 

using System;
using System.Text;

using Lex = MiniCppLex;
using Sem = MiniCppSem;

public class MiniCppSyn {
                   
  public static String MODULENAME = "MiniCppSyn";

  // --- G-code instructions ---
  public enum Instruction {
    T, TA, TC, TCA, NT, NTA, ANY, ANYA, EPS, EPSA, JMP, RET
  } // Instruction

  private const int ROOTPC     =  846;
  private const int LASTTERM   =   52;
  private const int LASTPRAGMA =   52;


  // --- other constants ---
  private const int MINERRDIST   =   3;
  private const int MAXSTACKLEN  = 100;
  private const int EOFTOK       =   0;
  private const int FIRSTNONTERM = LASTPRAGMA + 1;

  public class NTData {
    public int          startPC;
    public bool         del;
    public Sets.Set256  firstSet;
    public NTData(int startPC, bool del, Sets.Set256 firstSet) {
      this.startPC  = startPC;
      this.del      = del;
      this.firstSet = firstSet;
    } // NTData
  } // NTData
  
  private class GrammarTable {
    public int[]  header = {
      851, 52, 52, 82, 15, 0
    };
    public byte[] code = {
         5,   1,   9,   0,   1,  10,   1,   0,   5,  29,  17,   0,   2,  10,
         1,   0,   1,  31,  24,   0,  10,   1,   0,   8,   1,  15,  11,   0,
         1,   4,   5,   1,   2,  51,   2,  15,   4,   2,   4,   1,  30,  54,
         0,   2,  51,   5,  18,   4,   2,   7,  10,  40,   0,   0,  31,  11,
         0,  32,   1,   2,  64,   0,  11,   1,   3,  69,   0,  11,   1,   4,
        74,   0,  11,   1,  18,  82,   0,   2,  50,   2,  11,   1,  19,  78,
         0,  13,  10,  78,   0,   4,   5,   1,   1,  20,  97,   0,   2,  51,
         2,   5,   2, 105,   0,   3,   8,   2,  16,   1,  30, 130,   0,   1,
        20, 116,   0,   2,  51,   5,   5,   2, 124,   0,   6,   8,   2,  19,
        10, 108,   0,   0,  31,  11,   4,  28,   1,   1,  30, 146,   0,   4,
        28,   2,  10, 136,   0,   8,   3,  11,   1,  16, 154,   0,  11,   1,
         5, 159,   0,  11,   0,  17,  11,   0,  28,   5,   1, 172,   0,   1,
        10, 164,   0,   5,   3, 180,   0,   2,  10, 164,   0,   5,   7, 188,
         0,   3,  10, 164,   0,   0,  29,  11,   5,   8, 198,   0,   1,  23,
        11,   5,   9, 206,   0,   2,  10, 196,   0,   5,  10, 214,   0,   3,
        10, 196,   0,   5,  11, 222,   0,   4,  10, 196,   0,   5,  12, 230,
         0,   5,  10, 196,   0,   5,  13, 238,   0,   6,  10, 196,   0,   5,
        14, 246,   0,   7,  10, 196,   0,   5,  15, 254,   0,   8,  10, 196,
         0,   5,  16,   6,   1,   9,  10, 196,   0,   4,  17,  10,  10, 196,
         0,   0,  31,  11,   4,   6,   1,  11,   4,  18,   1,   0,  31,  11,
         0,   6,  13,   0,  22,   4,  18,   2,   0,  23,   4,   7,   3,   1,
         7,  47,   1,  16,   4,   7,   5,  11,   8,   4,  11,   0,   8,  13,
         0,  22,   4,  18,   2,   0,  23,   4,   7,   3,  11,   0,   9,  13,
         0,  31,  11,   0,  10,   0,  35,   2,  51,   1,   0,  31,  11,   0,
        11,   0,  36,   5,  18, 117,   1,   1,   1,  36, 114,   1,   5,  18,
       101,   1,   3,  10,  89,   1,   3,  52, 109,   1,   4,  10,  89,   1,
         0,  12,  10,  89,   1,   0,  31,  11,   3,  52, 125,   1,   2,  10,
        89,   1,   0,  12,  10,  89,   1,   0,  13,   0,  48,   0,  49,   2,
        51,   1,   0,  31,  11,   0,  14,   5,  18, 149,   1,   1,   0,  31,
        11,   4,  19,   1,   1,  32, 166,   1,  14,   4,  19,   8,  10, 155,
         1,   1,  37, 174,   1,  15,  10, 160,   1,   1,  38, 182,   1,  16,
        10, 160,   1,   1,  39, 190,   1,  17,  10, 160,   1,   1,  40, 198,
         1,  18,  10, 160,   1,   1,  41, 206,   1,  19,  10, 160,   1,   8,
         5,  11,   4,  20,   1,   1,  46, 223,   1,  14,   4,  20,   3,  10,
       212,   1,   8,   6,  11,   4,  21,   1,   1,  47, 240,   1,  14,   4,
        21,   3,  10, 229,   1,   8,   7,  11,   4,  22,   1,   1,  42,   1,
         2,  14,   4,  22,   8,  10, 246,   1,   1,  43,   9,   2,  15,  10,
       251,   1,   1,  25,  17,   2,  16,  10, 251,   1,   1,  34,  25,   2,
        17,  10, 251,   1,   1,  26,  33,   2,  18,  10, 251,   1,   1,  33,
        41,   2,  19,  10, 251,   1,   8,   8,  11,   1,  18,  74,   2,  13,
         4,  23,   3,   1,  18,  63,   2,  16,   4,  23,   6,  10,  52,   2,
         1,  19,  71,   2,  17,  10,  57,   2,   8,   9,  11,   1,  19,  49,
         2,  14,  10,  49,   2,   4,  24,   1,   1,  20,  96,   2,  14,   4,
        24,   5,  10,  85,   2,   1,  21, 104,   2,  15,  10,  90,   2,   1,
        24, 112,   2,  16,  10,  90,   2,   8,  10,  11,   1,  27, 120,   2,
        13,   4,  25,   2,  11,   1,   2, 129,   2,  11,   1,   3, 134,   2,
        11,   1,   4, 139,   2,  11,   3,  50, 145,   2,   1,  11,   1,  44,
       192,   2,  14,   2,  51,   4,   1,  48, 177,   2,   4,  18,   5,   0,
        49,   1,  44, 168,   2,  20,  11,   1,  45, 174,   2,  21,  11,   8,
        12,  11,   1,  22, 162,   2,   5,  26, 186,   2,   6,   0,  23,  19,
        10, 162,   2,   1,  45, 200,   2,  15,  10, 150,   2,   9,  11, 207,
         2,  10, 150,   2,   1,  15, 222,   2,   4,   5,  10,   0,  48,   4,
        18,  11,   0,  49,  11,   0,  22,   4,  18,  12,   0,  23,  11,   4,
        18,   1,   1,  30, 243,   2,   4,  18,   2,  10, 233,   2,   8,   3,
        11,   1,  20, 250,   2,   2,  51,   1,   1,  48,   4,   3,   0,  49,
        11,   8,  13,  11,   1,  16,  20,   3,   5,  27,  17,   3,   1,  11,
         8,  13,  11,   1,  17,  28,   3,   4,  27,   2,  11,   0,   5,  10,
        24,   3,   4,   5,   1,   1,  20,  40,   3,   2,  51,   2,   5,   2,
        58,   3,   3,  16,   5,   6,  54,   3,   7,   8,  15,  20,  11,   9,
        14,  65,   3,  10,  48,   3,   0,  22,   5,   4,  72,   3,   5,   0,
        23,  18,  10,  49,   3,   4,   0,   1,   0,   0,  11
    };
    public NTData[] nts = {
      new NTData(   1, true , new Sets.Set256(0x0022, 0x8003)),
      new NTData(  28, false, new Sets.Set256(0x0002)),
      new NTData(  57, false, new Sets.Set256(0x0000, 0x0000, 0x0001)),
      new NTData(  90, false, new Sets.Set256(0x0020, 0x0003)),
      new NTData( 133, false, new Sets.Set256(0x0020, 0x0003)),
      new NTData( 149, false, new Sets.Set256(0x0020, 0x0003)),
      new NTData( 162, false, new Sets.Set256(0x0000, 0x1000)),
      new NTData( 191, false, new Sets.Set256(0xef5c, 0x984c, 0x3000, 0x000c)),
      new NTData( 268, false, new Sets.Set256(0x0000, 0x8000)),
      new NTData( 271, false, new Sets.Set256(0x0000, 0x1000)),
      new NTData( 275, false, new Sets.Set256(0x801c, 0x084c, 0x3000, 0x000c)),
      new NTData( 281, false, new Sets.Set256(0x0040)),
      new NTData( 306, false, new Sets.Set256(0x0100)),
      new NTData( 320, false, new Sets.Set256(0x0200)),
      new NTData( 326, false, new Sets.Set256(0x0400)),
      new NTData( 336, false, new Sets.Set256(0x0800)),
      new NTData( 386, false, new Sets.Set256(0x2000)),
      new NTData( 398, false, new Sets.Set256(0x4000)),
      new NTData( 408, false, new Sets.Set256(0x801c, 0x084c, 0x3000, 0x000c)),
      new NTData( 465, false, new Sets.Set256(0x801c, 0x084c, 0x3000, 0x000c)),
      new NTData( 482, false, new Sets.Set256(0x801c, 0x084c, 0x3000, 0x000c)),
      new NTData( 499, false, new Sets.Set256(0x801c, 0x084c, 0x3000, 0x000c)),
      new NTData( 556, false, new Sets.Set256(0x801c, 0x084c, 0x3000, 0x000c)),
      new NTData( 594, false, new Sets.Set256(0x801c, 0x0840, 0x3000, 0x000c)),
      new NTData( 627, false, new Sets.Set256(0x801c, 0x0840, 0x3000, 0x000c)),
      new NTData( 636, false, new Sets.Set256(0x801c, 0x0040, 0x3000, 0x000c)),
      new NTData( 742, false, new Sets.Set256(0x801c, 0x084c, 0x3000, 0x000c)),
      new NTData( 758, false, new Sets.Set256(0x0000, 0x0010, 0x0000, 0x0008)),
      new NTData( 775, false, new Sets.Set256(0x0020, 0x0003)),
      new NTData( 801, false, new Sets.Set256(0x0020, 0x0003))
    };
    public Sets.Set256[] epsSets = {
      new Sets.Set256(0x0001),
      new Sets.Set256(0x0000, 0xc000),
      new Sets.Set256(0x0000, 0x0080),
      new Sets.Set256(0xeffe, 0xb84f, 0x3000, 0x000c),
      new Sets.Set256(0x0000, 0xc080, 0x0010, 0x0002),
      new Sets.Set256(0x0000, 0xc080, 0x03f1, 0x0002),
      new Sets.Set256(0x0000, 0xc080, 0x43f1, 0x0002),
      new Sets.Set256(0x0000, 0xc080, 0xc3f1, 0x0002),
      new Sets.Set256(0x0000, 0xc680, 0xcff7, 0x0002),
      new Sets.Set256(0x0000, 0xc68c, 0xcff7, 0x0002),
      new Sets.Set256(0x0000, 0x0000, 0x0000, 0x0008),
      new Sets.Set256(0x0000, 0xc7bc, 0xcff7, 0x0002),
      new Sets.Set256(0x0000, 0x4080),
      new Sets.Set256(0x0023, 0x9003),
      new Sets.Set256(0x0023, 0x8003)
    };
    public Sets.Set256[] anySets = {
      new Sets.Set256(0x0000)
    };
    public String[] names = {
      "end of file", "const", "false", "true", "nullptr", "bool", "if",
      "else", "while", "break", "cin", "cout", "endl", "delete", "return",
      "new", "void", "int", "+", "-", "*", "/", "(", ")", "%", "<", ">",
      "!", "{", "}", ",", ";", "=", ">=", "<=", ">>", "<<", "+=", "-=",
      "*=", "/=", "%=", "==", "!=", "++", "--", "||", "&&", "[", "]",
      "number", "ident", "string", "MiniCpp", "ConstDef", "Init", "VarDef",
      "FormParList", "Type", "Block", "Stat", "EmptyStat", "BlockStat",
      "ExprStat", "IfStat", "WhileStat", "BreakStat", "InputStat",
      "OutputStat", "DeleteStat", "ReturnStat", "Expr", "OrExpr",
      "AndExpr", "RelExpr", "SimpleExpr", "Term", "NotFact", "Fact",
      "ActParList", "FormParTypeRight", "FormPar", "MutDef"
    };
  } // GrammarTable;

  private static GrammarTable gt;

  private static int   pc;             // G-code counter
  private static int[] stk;            // stack of G-code return adresses
  private static int   sp;             // stack pointer
  private static bool  mustRead;       // next Lex.token to read
  private static int   altRoot;        // root of alternative chain
  private static bool  leaveOrEnter;   // leaveCnt > 0 OR enterCnt > 0
  private static int   leaveCnt;       // nr. of sem. proc. to leave
  private static int   enterCnt;       // nr. of sem. proc. to enter
  private static int   errDist;        // current error distance
  private static int[] stkAtErr;       // saved stack at error position
  private static int[] newSP;          // new stack length
  private static int[] newPC;          // pc after recovery


  public static void MiniCppSynMethod(Utils.ModuleAction action, out String moduleName) {
  //-----------------------------------|----------------------------------------
    moduleName = MODULENAME;
    switch (action) {
      case Utils.ModuleAction.getModuleName:
        return;
      case Utils.ModuleAction.initModule:
        Errors.InstallStopParsingMethod(StopParsing);
        gt       = new GrammarTable();
        stk      = new int[MAXSTACKLEN];
        stkAtErr = new int[MAXSTACKLEN];
        newSP    = new int[LASTTERM + 1];
        newPC    = new int[LASTTERM + 1];
        break;
      case Utils.ModuleAction.resetModule:
        break;
      case Utils.ModuleAction.cleanupModule:
        gt       = null;
        stk      = null;
        stkAtErr = null;
        newSP    = null;
        newPC    = null;
        break;
    } // switch
  } // MiniCppSynMethod

  private static void Push(int pc) {
    if (sp < MAXSTACKLEN)
      stk[sp++] = pc;
    else
      Errors.Restriction(MODULENAME, "Push", "parser stack overflow");
  } // Push

  private static int Pop() {
    if (sp > 0)
      return stk[--sp];
    else {
      Errors.CompilerError(MODULENAME, "Pop", "parser stack underflow");
      return 0;
    } // else
  } // Pop

  private static void GetNextToken() {
    for (;;) {
      Lex.GetToken(); // updates Lex.token
      if (Lex.token <= LASTTERM)
        return;
  #if !CONTPRAGMAACTS
      if (NumOfSynErrors() == 0)
  #endif
        Sem.pragmaMethods[Lex.token - LASTTERM - 1]();
    } // for
  } // GetNextToken

  private static int AdrAt(int pc) {
    return (gt.code[pc - 1] |
           (gt.code[pc    ] << 8));
  } // AdrAt

  private static void AdjustPC(ref int pc) {
    if (pc == 0)
      return;
    for (;;)
      switch ((Instruction)gt.code[pc - 1]) {
        case Instruction.T:
        case Instruction.TA:
        case Instruction.TC:
        case Instruction.TCA:
        case Instruction.NT:
        case Instruction.NTA:
        case Instruction.ANY:
        case Instruction.ANYA:
        case Instruction.EPS:
        case Instruction.EPSA:
          return;
        case Instruction.JMP:
          pc = AdrAt(pc + 1);
          break;
        case Instruction.RET:
          pc = 0;
          return;
        default:
          pc++; // syn. or sem. action
          break;
      } // switch
  } // AdjustPC

  private static void GetSymInstr(int pc, 
                        out int opcode, out int sy, out int nextPC, out int altPC) {
    opcode = gt.code[pc - 1];
    nextPC = 0; // dummy init
    altPC  = 0; // dummy init 
    switch ((Instruction)opcode) {
      case Instruction.T:
      case Instruction.TA:
      case Instruction.TC:
      case Instruction.TCA:
      case Instruction.ANYA:
      case Instruction.EPS:
      case Instruction.EPSA:
        sy = gt.code[pc];
        break;
      case Instruction.NT:
      case Instruction.NTA:
        sy = gt.code[pc] + FIRSTNONTERM;
        break;
      default: // ANY, JMP, RET, SEM, SYN 
        sy = 0;
        break;
    } // switch
    switch ((Instruction)opcode) {
      case Instruction.T:
      case Instruction.EPS:
        nextPC = pc + 2;
        altPC = 0;
        break;
      case Instruction.TC:
      case Instruction.NT:
        nextPC = pc + 3;
        altPC = 0;
        break;
      case Instruction.TA:
      case Instruction.ANYA:
      case Instruction.EPSA:
        nextPC = pc + 4;
        altPC = AdrAt(pc + 2);
        break;
      case Instruction.TCA:
      case Instruction.NTA:
        nextPC = pc + 5;
        altPC = AdrAt(pc + 2);
        break;
      case Instruction.ANY:
        nextPC = pc + 1;
        altPC = 0;
        break;
    } // switch
    AdjustPC(ref nextPC);
    AdjustPC(ref altPC);
  } // GetSymInstr

  private static void Fill(int pc, int sp) {
    int opcode, sy, nextPC, altPC, i;
    AdjustPC(ref pc);
    while (pc > 0) {
      GetSymInstr(pc, out opcode, out sy, out nextPC, out altPC);
      switch ((Instruction)opcode) {
        case Instruction.T:
        case Instruction.TA:
        case Instruction.TC:
        case Instruction.TCA:
          newPC[sy] = pc;
          newSP[sy] = sp;
          break;
        case Instruction.NT:
        case Instruction.NTA:
          for (i = 0; i <= LASTTERM; i++)
            if (Sets.member((ushort)i, gt.nts[sy - FIRSTNONTERM].firstSet)) {
              newPC[i] = pc;
              newSP[i] = sp;
            }
          if (gt.nts[sy - FIRSTNONTERM].del)
            Fill(nextPC, sp);
          break;
        case Instruction.EPS:
        case Instruction.EPSA:
          Fill(nextPC, sp);
          break;
      } // switch
      pc = altPC;
    } // while
  } // Fill

  private static void FillSucc(int pc, int sp) {
    int opcode, sy, nextPC, altPC;
    AdjustPC(ref pc);
    while (pc > 0) {
      GetSymInstr(pc, out opcode, out sy, out nextPC, out altPC);
      if (nextPC > 0)
        Fill(nextPC, sp);
      pc = altPC;
    } // while
  } // FillSucc

  private static void Triple(int altRoot) {
    for (int i = 0; i <= LASTTERM; i++) { // clear triple list
      newPC[i] = 0;
      newSP[i] = 0;
    } // for
    for (int i = 1; i <= sp; i++) {       // successors of stacked nt's
      FillSucc(stk[i - 1], i - 1);
      Fill(stk[i - 1], i - 1);
    } // for
    FillSucc(altRoot, sp);                // successors of alt. chain
    Fill(altRoot, sp);                    // current alt. chain
  } // Triple

  private static void HandleSynErr(ref int pc, ref int altRoot) {
    int     opcode, sy, nextPC, altPC, adjPC, nwSP, eq;
    bool    nameInMsg;
    String  name;
    StringBuilder msg = new StringBuilder();
    leaveOrEnter = false;
    if (errDist >= MINERRDIST) { // --- primary error
      nameInMsg = false;
      adjPC = altRoot;
      AdjustPC(ref adjPC);
      while (adjPC > 0) {
        GetSymInstr(adjPC, out opcode, out sy, out nextPC, out altPC);
        if ( (Instruction)opcode >= Instruction.T  && 
             (Instruction)opcode <= Instruction.NTA  ) {
          if ( gt.names[sy].IndexOf(' ') >= 0 || 
               !Char.IsLetter(gt.names[sy][0]) )
            name = "\"" + gt.names[sy] + "\"";
          else
            name = gt.names[sy];
          if (name.IndexOf('{') > -1) // prevent formatting problems in Errors.SynError
            name = name.Replace("{", "{{");
          if (name.IndexOf('}') > -1) // prevent formatting problems in Errors.SynError
            name = name.Replace("}", "}}");
          if (msg.Length + name.Length + 2 > 80) {
            msg.Append(" ...");
            break;
          } // if
          if (!nameInMsg) {
            nameInMsg = true;
            msg.Append("expected: " + name);
          } else {
            msg.Append(", ");
            msg.Append(name);
          } // else
        } // if
        adjPC = altPC;
      } // while
      if (!nameInMsg)
        msg.Append("syntax error");
      Errors.SynError(Lex.tokenLine, Lex.tokenCol, msg.ToString(), "");
      Triple(altRoot);
      while (newPC[Lex.token] == 0) {
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter)
          return;
      } // while
      nwSP = newSP[Lex.token];
      leaveCnt = sp - nwSP;
      enterCnt = 0;
      Array.Copy(stk, stkAtErr, stk.Length); // save stack
    } else {                 // --- spurios error
      if (errDist == 0) {    // no token used since last error
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter)
          return;
      } // if
      while (newPC[Lex.token] == 0) {
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter) 
          return;
      } // while
      nwSP = newSP[Lex.token];
      eq = 2;                // compare stkAtErr and stk
      while (eq <= sp && eq <= nwSP && stkAtErr[eq - 1] == stk[eq - 1])
        eq++;
      eq--;                  // stacks are equal up to index eq
      leaveCnt = sp - eq;
      if (nwSP < eq) {
        leaveCnt = sp - nwSP;
        enterCnt = 0;
      } else if (nwSP > eq)
        enterCnt = nwSP - eq;
      else // nwSP == eq
        enterCnt = 0;
      Array.Copy(stkAtErr, stk, stk.Length); // restore stack
    } // else
    leaveOrEnter = (leaveCnt + enterCnt) > 0;
    sp = nwSP;
    pc = newPC[Lex.token];
    altRoot = pc;
    errDist = 0;
  } // HandleSynErr

  public static int Interpret() {
  //-----------------------------------|----------------------------------------
    int actionNr, opcode, currPC;
    for (;;) {
      opcode = gt.code[pc - 1];
      if ( !leaveOrEnter && mustRead && 
           (Instruction)opcode <= Instruction.EPSA ) {
        GetNextToken();
        mustRead = false;
        errDist++;
        altRoot = pc;
      } // if
      if (leaveOrEnter) {
        if (leaveCnt > 0) {
          leaveCnt--;
          actionNr = 0;
        } else { // enterCnt > 0
          enterCnt--;
          actionNr = gt.code[stkAtErr[sp - enterCnt - 1] - 2];
        } // else
        leaveOrEnter = (leaveCnt + enterCnt) > 0;
        return actionNr;
      } // if
      pc++;
      switch ((Instruction)opcode) {
        case Instruction.T:
          if (Lex.token == gt.code[pc - 1])
            if (Lex.token != EOFTOK) {
              pc++;
              mustRead = true;
            } else {
              return 0;
            } // else
          else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.TA:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 3;
            mustRead = true;
          } else
            pc = AdrAt(pc + 1); // try alternative
          break;
        case Instruction.TC:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 2;
            mustRead = true;
            actionNr = gt.code[pc - 2];
            return actionNr;
          } else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.TCA:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 4;
            mustRead = true;
            actionNr = gt.code[pc - 2];
            return actionNr;
          } else
            pc = AdrAt(pc + 1); // try alternative
          break;
        case Instruction.NT:
          currPC = gt.code[pc - 1];
          if ( gt.nts[currPC].del || 
               Sets.member((ushort)Lex.token, gt.nts[currPC].firstSet) ) {
            pc++;
            actionNr = gt.code[pc - 1];
            Push(pc + 1);
            pc = gt.nts[currPC].startPC;
            altRoot = pc;
            return actionNr;
          } else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.NTA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.nts[currPC].firstSet)) {
            pc += 3;
            actionNr = gt.code[pc - 1];
            Push(pc + 1);
            pc = gt.nts[currPC].startPC;
            altRoot = pc;
            return actionNr;
          } else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.ANY:
          mustRead = true;
          break;
        case Instruction.ANYA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.anySets[currPC - 1])) {
            pc += 3;
            mustRead = true;
          } else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.EPS:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.epsSets[currPC - 1]))
            pc++;
          else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.EPSA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.epsSets[currPC - 1]))
            pc += 3;
          else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.JMP:
          pc = AdrAt(pc);
          break;
        case Instruction.RET:
          pc = Pop();
          altRoot = pc;
          return 0;
        default:
          if (opcode <= 128) {
  #if !CONTSEMACTS
          if (Errors.NumOfSynErrors() == 0)
  #endif
            return opcode - 12;
          } else // opcode > 128
            return opcode - 128;
          break;
      } // switch
    } // for
  } // Interpret


  public static bool Parse() {
  //-----------------------------------|----------------------------------------
    Lex.InitLex();
    sp           = 0;
    pc           = ROOTPC;
    altRoot      = pc;
    mustRead     = true;
    errDist      = MINERRDIST + 1;
    leaveOrEnter = false;
    leaveCnt     = 0;
    enterCnt     = 0;
    Sem.StartSem();
    return Errors.NumOfSynErrors() == 0;
  } // Parse


  public static void StopParsing() {
  //-----------------------------------|----------------------------------------
    leaveCnt     = sp + 1;
    enterCnt     = 0;
    leaveOrEnter = true;
  } // StopParsing

} // MiniCppSyn
 
// End of MiniCppSyn.cs
//=====================================|========================================